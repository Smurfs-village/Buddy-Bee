import CreatePageLayout from "../../components/Layout/CreatePageLayout";

const CreateFundingProjectPage = ({ user }) => {
  return <CreatePageLayout type="funding" user={user} />;
};

export default CreateFundingProjectPage;
